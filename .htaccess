RewriteEngine On

# Biarkan file dan folder fisik diakses langsung (CSS, JS, gambar, dll)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Arahkan semua request lain ke index.php
RewriteRule ^ index.php [QSA,L]
